{
  "name": "murajah-data-pipeline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Build-time scripts: chunk + compress Quran data/assets from source/ into app/public.",
  "scripts": {
    "data:build": "node src/build.mjs",
    "test": "node --test"
  }
}
